<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MyHwMon1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyHwMon1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MyHwMon1/OpenHardwareMonitorLib-Aktualisierung jede Sekunde ">

    <!--
    In einer WinUI 3 App (C# oder C++) sorgt dieser Block für das moderne, halbtransparente Erscheinungsbild des Fensterhintergrunds unter Windows 11.
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

    </Grid>-->

    <StackPanel Padding="20" Spacing="50">
        <!--<TextBlock Text="Hardware Sensoren (Aktualisierung minütlich)" Style="{StaticResource TitleTextBlockStyle}"/>-->

        <!-- Liste für die 14 Text-Controls -->
        <ItemsControl ItemsSource="{x:Bind SensorValues, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{Binding}" Margin="0,2" FontSize="16" FontFamily="Consolas"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>

</Window>



